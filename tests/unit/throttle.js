define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: 'dojo/throttle',

		'TODO: convert tests': function () {
			assert.fail();
		}
	});
});
