define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: 'dojo/_base/window',

		'TODO: convert tests': function () {
			assert.fail();
		}
	});
});
