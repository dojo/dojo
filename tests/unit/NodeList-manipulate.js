define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: 'dojo/NodeList-manipulate',

		'TODO: convert tests': function () {
			assert.fail();
		}
	});
});
