define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: 'dojo/dom-style',

		'TODO: convert tests': function () {
			assert.fail();
		}
	});
});
