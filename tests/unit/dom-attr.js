define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: 'dojo/debounce',

		'TODO: convert tests': function () {
			assert.fail();
		}
	});
});
